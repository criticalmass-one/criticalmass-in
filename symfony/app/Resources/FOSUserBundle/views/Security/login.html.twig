{% extends 'CalderaCriticalmassSiteBundle:Template:StandardTemplate.html.twig' %}

{% block title %}Login{% endblock %}

{% block breadcrumb %}
    <li class="active">Login</li>
{% endblock %}

{% block content %}
    <div class="container">
        {% if error %}
            <div class="row">
                <div class="col-md-10 col-md-offset-1">
                    <div class="alert alert-danger alert-error">{{ error.messageKey|trans(error.messageData, 'security') }}</div>
                </div>
            </div>
        {% endif %}

        <a href="{{ path('hwi_oauth_service_redirect', {'service': 'facebook' }) }}" alt="Sign in with Facebook">Sign in with Facebook</a>
        <a href="{{ path('hwi_oauth_service_redirect', {'service': 'google' }) }}" alt="Sign in with Facebook">Sign in with Google</a>
        <a href="{{ path('hwi_oauth_service_redirect', {'service': 'strava' }) }}" alt="Sign in with Facebook">Sign in with Strava</a>
        <a href="{{ path('hwi_oauth_service_redirect', {'service': 'twitter' }) }}" alt="Sign in with Facebook">Sign in with Twitter</a>
        <a href="{{ path('hwi_oauth_service_redirect', {'service': 'runkeeper' }) }}" alt="Sign in with Facebook">Sign in with Runkeeper</a>

        <div class="row">
            <div class="col-md-10 col-md-offset-1">
                <div class="btn-group pull-right margin-top-medium">
                    <a href="{{ path('fos_user_resetting_request') }}" class="btn btn-danger">
                        <i class="fa fa-life-ring"></i>
                        Kennwort vergessen?
                    </a>
                    <a href="{{ path('fos_user_registration_register') }}" class="btn btn-success">
                        <i class="fa fa-user-plus"></i>
                        Registrieren
                    </a>

                </div>
                <h1>Einloggen und losfahren!</h1>

                <p>Hier kannst du dich mit deinem criticalmass.in-Benutzerkonto einloggen. Gültig sind alle Benutzerkonten, die seit Anfang 2013 registriert worden sind. Falls du dein Kennwort vergessen hast, kannst du dir <a href="{{ path('fos_user_resetting_request') }}">ein neues zusenden lassen</a>. Und falls du noch kein Benutzerkonto hast, kannst du dich <a href="{{ path('fos_user_registration_register') }}">hier registrieren</a>.</p>
            </div>
        </div>

        <div class="row">
            <div class="col-md-10 col-md-offset-1">
                <form action="{{ path("fos_user_security_check") }}" method="post" role="form" class="form-horizontal">
                    <input type="hidden" name="_csrf_token" value="{{ csrf_token }}"/>

                    <div class="form-group">
                        <label for="username" class="col-sm-4 control-label">Dein Benutzername:</label>
                        <div class="col-sm-8">
                            <input type="text" class="form-control" id="username" name="_username" value="{{ last_username }}" required="required"/>
                        </div>
                    </div>

                    <div class="form-group">
                        <label for="password" class="col-sm-4 control-label">Dein Kennwort:</label>
                        <div class="col-sm-8">
                            <input type="password" class="form-control" id="password" name="_password" required="required"/>
                        </div>
                    </div>

                    <div class="form-group">
                        <div class="col-sm-offset-4 col-sm-8">
                            <div class="checkbox control-group">
                                <label for="remember_me">
                                    <input type="checkbox" id="remember_me" name="_remember_me" value="on"/> eingeloggt bleiben
                                </label>
                            </div>
                        </div>
                    </div>

                    <div class="form-group">
                        <div class="col-sm-offset-4 col-sm-8">
                            <button type="submit" id="_submit" name="_submit" class="btn btn-success pull-right">
                                <i class="fa fa-bicycle"></i>
                                Ride on
                            </button>
                        </div>
                    </div>
                </form>
            </div>
        </div>
    </div>
{% endblock %}