{% extends 'CalderaCriticalmassDesktopBundle:Common:StandardTemplate.html.twig' %}

{% block title %}Deine Tracks{% endblock %}

{% block content %}
    <div class="container">
        <h1>Hier warst du schon mal.</h1>
        <p class="lead">criticalmass.in zeigt dir seit 2012 den Weg zur nächsten Critical Mass. Und wir sind noch lange nicht fertig.</p>

        <a href="{{ path('caldera_criticalmass_statistic_track_upload') }}" class="btn btn-success">Track hochladen</a>

        <table class="table">
            <tr>
                <th>Datum</th>
                <th>Stadt</th>
                <th>Tour</th>
                <th>Beginn</th>
                <th>Ende</th>
                <th>Wegpunkte</th>
            </tr>
            {% for track in tracks %}
            <tr>
                <td>{{ track.getRide().getDateTime().format('d. m. Y') }}</td>
                <td>{{ track.getRide().getCity().getCity() }}</td>
                <td>{% if track.getRide().getTitle() %}
                {{ track.getRide().getTitle() }}
                {% else %}
                {{ track.getRide().getCity().getTitle() }}
                {% endif %}</td>
                <td>{{ track.getStartDateTime().format('H:i') }} Uhr</td>
                <td>{{ track.getEndDateTime().format('H:i') }} Uhr</td>
                <td>{{ track.getPoints() }}</td>
            </tr>
            {% endfor %}
        </table>
    </div>
{% endblock %}