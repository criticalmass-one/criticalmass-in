define([], function() {
        var CityList = function(context, settings) {
            this.context = context;
            this.settings = settings;
        };
        
        CityList.prototype.$$init = function() {

            
        }
    }
);