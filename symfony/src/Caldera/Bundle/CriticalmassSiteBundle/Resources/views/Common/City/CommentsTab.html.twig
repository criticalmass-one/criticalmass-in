<div class="tab-pane fade in" id="timelineTab">
    <div class="row">
        <div class="col-md-8">
            {{ render(controller('CalderaCriticalmassSiteBundle:Post:list', { 'cityId': city.getId() })) }}
        </div>
        <div class="col-md-4">
            {{ render(controller('CalderaCriticalmassSiteBundle:Post:write', { 'cityId': city.getId() })) }}
        </div>
    </div>
</div>