{% extends 'CalderaCriticalmassSiteBundle:Template:StandardTemplate.html.twig' %}

{% block title %}Fotogalerie{% endblock %}

{% block breadcrumb %}
    <li>
        <a href="{{ path('caldera_criticalmass_desktop_city_list') }}">
            Städteliste
        </a>
    </li>
    <li class="active">
        Fotogalerie
    </li>
{% endblock %}

{% block content %}
    <div class="container">
        <div class="row">
            <div class="col-md-12">
                <h1>
                    Fotogalerie
                </h1>
            </div>
        </div>

        <div class="row">
            <div class="col-md-12">
                <p class="lead">
                    Neugierig geworden? Hier siehst du ein paar zufällig ausgewählte Fotos aus früheren Critical-Mass-Touren.
                </p>
            </div>
        </div>

        <div class="row">
            <div class="photo-list">
            {% for photo in photos %}
                <div class="col-xs-6 col-sm-4 col-md-3 col-lg-3">
                    <div class="photo margin-bottom-medium text-center">
                        <a href="{{ path(photo) }}">
                            <img src="{{ vich_uploader_asset(photo, 'imageFile')|imagine_filter('gallery_photo_thumb') }}" />
                        </a>
                    </div>
                </div>
            {% endfor %}
            </div>
        </div>
    </div>
{% endblock %}
