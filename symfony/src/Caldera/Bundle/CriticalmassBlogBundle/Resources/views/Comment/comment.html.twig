<div class="row comment m-b-1">
    <div class="col-xs-2">
        <img class="img-circle img-fluid" src="{{ gravatarUrl(comment.user, 256) }}" />
    </div>

    <div class="col-xs-10">
        <h4 class="username">
            {% if comment.getRide() %}
                {{ comment.getUser().getUsername() }} schreibt zu <a href="{{ path(comment.ride) }}">{{ comment.ride.fancyTitle }}</a>:
            {% elseif comment.getCity() %}
                {{ comment.getUser().getUsername() }} schreibt über <a href="{{ path(comment.city) }}">{{ comment.city.title }}</a>:
            {% else %}
                {{ comment.getUser().getUsername() }} schreibt:
            {% endif %}
        </h4>

        <time>{{ comment.dateTime|date('d.m.Y H:i') }}&nbsp;Uhr</time>

        {% if comment.obfuscated %}
        <div class="alert alert-warning" role="alert">
            <strong>Schade!</strong> Dieser Kommentar enthält unangemessene Inhalte. <button class="btn btn-sm btn-secondary pull-right" onclick="$(this).parent().parent().find('.obfuscate').removeClass('obfuscate');$(this).remove();"><i class="fa fa-eye" aria-hidden="true"></i> Lesen</button>
        </div>
        <div class="message obfuscate">
            {{ comment.message|markdown }}
        </div>
        {% else %}
        <div class="message">
            {{ comment.message|markdown }}
        </div>
        {% endif %}
    </div>
</div>