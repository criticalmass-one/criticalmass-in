<!DOCTYPE html>
<html lang="de">
<head>
	<title>{{ city.getTitle }}</title>
	<meta charset="utf-8" />
	<meta name="viewport" content="width=320,user-scalable=no" />
	<meta name="apple-mobile-web-app-capable" content="yes" />
	{% stylesheets '@CalderaCriticalmassBundle/Resources/public/css/*.css' filter="cssrewrite" %}
	<link rel="stylesheet" href="{{ asset_url }}" type="text/css" />
	{% endstylesheets %}
</head>
<body>
	<header><h1>{{ city.getTitle }}</h1></header>
	<section id="content">
		<h2>Nächste Tour</h2>
		<p>Datum: {{ ride.getDate|date('d.m.Y') }}</p>
		<p>Uhrzeit: {{ ride.getTime|date('H:i') }}</p>
		{% if ride.getLocation == "tba" %}
			<p class="locationunknown">Der Treffpunkt ist noch nicht bekannt :( Schau später noch mal vorbei.</p>
		{% else %}
			<p>Treffpunkt: {{ ride.getLocation }}</p>
			{{ ride.getMap|raw }}
		{% endif %}
	</section>
	<footer>
		{% if city.getUrl %}<a href="{{ city.getUrl }}"><img class="icon" src="{{ asset('bundles/calderacriticalmass/images/rss.png') }}" alt="RSS-Feed" /></a>{% endif %}
		{% if city.getTwitter %}<a href="{{ city.getTwitter }}"><img class="icon" src="{{ asset('bundles/calderacriticalmass/images/twitter.png') }}" alt="Twitter" /></a>{% endif %}
		{% if city.getFacebook %}<a href="{{ city.getFacebook }}"><img class="icon" src="{{ asset('bundles/calderacriticalmass/images/facebook.png') }}" alt="faebook" /></a>{% endif %}
	</footer>
</body>
</html>