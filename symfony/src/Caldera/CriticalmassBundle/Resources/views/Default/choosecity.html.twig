<!DOCTYPE html>
<html lang="de">
<head>
	<title>criticalmass.in</title>
	<meta charset="utf-8" />
	<meta name="viewport" content="width=320,user-scalable=no" />
	<meta name="apple-mobile-web-app-capable" content="yes" />
	<style type="text/css">
	body
	{
		font: 1em Helvetica, Arial, Sans-Serif;
		margin: 0;
		padding: 0;
	}

	header, section#content, footer
	{
		margin: 0 auto;
		width: 300px;
		padding: 10px;
	}

	header
	{
		background: black;
	}

	header h1
	{
		color: white;
		font-size: 25px;
		text-align: center;
	}

	footer
	{
		text-align: center;
	}

	footer img.icon
	{
		width: 70px;
		height: 70px;
		margin: 10px;
	}

	p.locationunknown
	{
		border: 1px red solid;
		background: #fdd;
		padding: 3px;
	}
	</style>
</head>
<body>
	<ul>
	{% for city in cities %}
	<li><a href="{{ url('caldera_criticalmass_showcity', {'city': city['city'].getCity }) }}">{{ city['city'].getTitle }}</a> (Entfernung: {{ city['distance']|number_format(2) * 75 }} Kilometer)</li>
	{% endfor %}
	</ul>
</body>
</html>