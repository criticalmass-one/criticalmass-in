{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Ride list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Date</th>
                <th>Time</th>
                <th>Location</th>
                <th>Map</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('admin_ride_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{% if entity.date %}{{ entity.date|date('Y-m-d H:i:s') }}{% endif %}</td>
                <td>{{ entity.time }}</td>
                <td>{{ entity.location }}</td>
                <td>{{ entity.map }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('admin_ride_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('admin_ride_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('admin_ride_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
