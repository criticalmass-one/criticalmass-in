{% extends 'CalderaCriticalmassBundle:Default:layout.html.twig' %}

{% block title %}{{ city.getTitle }}{% endblock %}

{% block body %}
	<section id="content" data-role="content">
		<div id="map-canvas" style="width: 300px; height: 300px; margin: 10px auto;"></div>

		<div data-role="fieldcontain">
			<label for="flip-gps-sender">GPS-Übertragung:</label>
				<select name="flip-gps-sender" id="flip-gps-sender" data-role="slider">
					<option value="off"{% if app.user.sendGPSInformation == 0 %} selected="selected"{% endif %}>Aus</option>
					<option value="on"{% if app.user.sendGPSInformation == 1 %} selected="selected"{% endif %}>An</option>
				</select>
			<label for="slider-gps-interval">GPS-Intervall in Sekunden:</label>
				<input type="range" name="slider-gps-interval" id="slider-gps-interval" value="{{ app.user.gpsinterval }}" min="1" max="300" />
		</div>
		
	</section>
{% endblock %}
