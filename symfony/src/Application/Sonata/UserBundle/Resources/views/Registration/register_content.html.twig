{% extends 'CalderaCriticalmassDesktopBundle:Common:StandardTemplate.html.twig' %}

{% block title %}Registrierung{% endblock %}

{% block content %}
    <div class="container">
        <h1>Auf die Räder, fertig, los.</h1>
        <p class="lead">Registriere dir hier dein persönliches criticalmass.in-Benutzerkonto.</p>

        <p>Die Übertragung deiner Daten erfolgt selbstverständlich verschlüsselt. Und natürlich geben wir deine Daten auch nicht weiter.</p>

        <form action="{{ path('fos_user_registration_register') }}" {{ form_enctype(form) }} method="POST" class="fos_user_registration_register form-horizontal">
            <div class="form-group">
                <label for="username" class="col-sm-4 control-label">Dein gewünschter Benutzername:</label>
                <div class="col-sm-8">
                    {{ form_widget(form.username, {'attr': {'class': 'form-control'}}) }}
                </div>
            </div>

            <div class="form-group">
                <label for="email" class="col-sm-4 control-label">Deine E-Mail-Adresse:</label>
                <div class="col-sm-8">
                    {{ form_widget(form.email, {'attr': {'class': 'form-control'}}) }}
                </div>
            </div>

            <div class="form-group control-group">
                <label for="password" class="col-sm-4 control-label">Dein Kennwort:</label>
                <div class="col-sm-8">
                    {{ form_widget(form.plainPassword.first, {'attr': {'class': 'form-control'}}) }}
                </div>
            </div>

            <div class="form-group control-group">
                <label for="password" class="col-sm-4 control-label">Gleich noch mal dein Kennwort:</label>
                <div class="col-sm-8">
                    {{ form_widget(form.plainPassword.second, {'attr': {'class': 'form-control'}}) }}
                </div>
            </div>

            <div class="form-group">
                <div class="col-sm-offset-4 col-sm-8">
                    <input type="submit" value="Aufsatteln" class="btn btn-primary" />
                </div>
            </div>
        </form>
    </div>
{% endblock %}