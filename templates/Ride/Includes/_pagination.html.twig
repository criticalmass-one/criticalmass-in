{% set previous_ride = previous_entity(ride) %}
{% set next_ride = next_entity(ride) %}

<nav aria-label="Tour Navigation">
    <ul class="pagination justify-content-between">

        {# Previous #}
        <li class="page-item {% if not previous_ride %}disabled{% endif %}">
            <a class="page-link" href="{{ previous_ride ? object_path(previous_ride) : '#' }}">
                &larr; Vorige Tour
            </a>
        </li>

        {# Next #}
        <li class="page-item {% if not next_ride %}disabled{% endif %}">
            <a class="page-link" href="{{ next_ride ? object_path(next_ride) : '#' }}">
                Nächste Tour &rarr;
            </a>
        </li>

    </ul>
</nav>
