<nav aria-label="Ride navigation" class="text-center m-4">
    <ul class="pagination justify-content-center">
        {% if previousRide %}
        <li class="page-item">
            <a class="page-link" href="{{ objectPath(previousRide) }}">
                &larr; Vorige Tour
            </a>
        </li>
        {% else %}
        <li class="page-item disabled">
            <a class="page-link" href="#">
                &larr; Vorige Tour
            </a>
        </li>
        {% endif %}

        {% if nextRide %}
        <li class="page-item">
            <a class="page-link" href="{{ objectPath(nextRide) }}">
                Nächste Tour &rarr;
            </a>
        </li>
        {% else %}
        <li class="page-item disabled">
            <a class="page-link" href="#">
                Nächste Tour &rarr;
            </a>
        </li>
        {% endif %}
    </ul>
</nav>
