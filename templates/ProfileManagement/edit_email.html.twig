{% extends 'Template/StandardTemplate.html.twig' %}
{% import 'Template/Macros/breadcrumb.html.twig' as breadcrumb %}

{% form_theme userEmailForm 'Form/theme.html.twig' %}

{% block title %}{{ 'profile.email.title'|trans }}{% endblock %}

{% block breadcrumb %}
    {{ breadcrumb.item('profile.manage.breadcrumb'|trans, path('criticalmass_user_usermanagement')) }}
    {{ breadcrumb.active('profile.email.breadcrumb'|trans) }}
{% endblock %}

{% block content %}
    <div class="container">
        <div class="row">
            <div class="col-md-6 col-md-offset-3">
                <h2>
                    {{ 'profile.email.heading'|trans }}
                </h2>

                <p class="lead">
                    {{ 'profile.email.lead'|trans }}
                </p>
            </div>
        </div>

        {{ form_start(userEmailForm) }}

        <div class="row">
            <div class="col-md-6 col-md-offset-3">
                <div class="panel panel-default">
                    <div class="panel-heading">
                        {{ 'profile.email.panel_title'|trans }}
                    </div>
                    <div class="panel-body form-group form-group{% if form_errors(userEmailForm.email) %} has-error has-feedback{% endif %}">
                        <p>
                            {{ 'profile.email.explanation'|trans }}
                        </p>

                        {{ form_errors(userEmailForm.email) }}

                        <label class="control-label" for="">
                            {{ 'profile.email.label'|trans }}
                        </label>
                        {{ form_widget(userEmailForm.email, { 'attr' : { 'class': 'form-control'} }) }}
                    </div>
                </div>
            </div>
        </div>

        <div class="row">
            <div class="col-md-6 col-md-offset-3 text-center">
                <div class="btn-group" role="group">
                    <a href="{{ path('criticalmass_user_usermanagement') }}" class="btn btn-secondary">
                        {{ 'common.cancel'|trans }}
                    </a>

                    <button type="submit" class="btn btn-primary">
                        {{ 'common.save'|trans }}
                    </button>
                </div>
            </div>
        </div>

        {{ form_end(userEmailForm) }}
    </div>
{% endblock %}
