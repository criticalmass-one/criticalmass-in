{% extends 'Template/StandardTemplate.html.twig' %}
{% import 'Template/Macros/breadcrumb.html.twig' as breadcrumb %}

{% block title %}Farbe wählen{% endblock %}

{% block breadcrumb %}
    {{ breadcrumb.item('Dein Benutzerkonto', path('criticalmass_user_usermanagement')) }}
    {{ breadcrumb.active('Farbe wählen') }}
{% endblock %}

{% block content %}
    <div class="container">

        <div class="row">
            <div class="col-md-6 col-md-offset-3 text-center">
                <h2>
                    Deine Benutzerfarbe
                </h2>

                <p>
                    Hier kannst du eine Farbe wählen, mit der dein Benutzerkonto auf criticalmass.in repräsentiert wird. Beispielsweise werden von dir hochgeladene Tracks in dieser Farbe angezeigt.
                </p>
            </div>
        </div>

        {{ form_start(profileColorForm) }}
        <div class="row">
            <div class="col-md-4 col-md-offset-4 text-center">
                {{ form_row(profileColorForm.color) }}
            </div>
        </div>

        <div class="row margin-top-medium">
            <div class="col-md-6 col-md-offset-3 text-center">
                <div class="btn-group">
                    {{ form_widget(profileColorForm.submit, { label: 'Speichern', attr: { class: 'btn btn-primary'}}) }}

                    <a href="{{ path('criticalmass_user_usermanagement') }}" class="btn btn-secondary">
                        Abbrechen
                    </a>
                </div>
            </div>
        </div>
        {{ form_end(profileColorForm) }}
    </div>
{% endblock %}
