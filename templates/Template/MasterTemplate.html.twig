{% if entrypoint is not defined %}
    {% set entrypoint = 'app' %}
{% endif %}

<!DOCTYPE html>
<html {{ sonata_seo_html_attributes() }}>
<head {{ sonata_seo_head_attributes() }}>

    {% if block('title') %}
        <title>{% block title %}{% endblock %} — criticalmass.in</title>
    {% else %}
        {{ sonata_seo_title() }}
    {% endif %}

    <meta http-equiv="X-UA-Compatible" content="IE=edge"/>
    <meta name="viewport" content="initial-scale=1,user-scalable=no,maximum-scale=1,width=device-width"/>
    <meta name="mobile-web-app-capable" content="yes"/>
    <meta name="apple-mobile-web-app-capable" content="yes"/>

    {{ sonata_seo_metadatas() }}
    {{ sonata_seo_link_canonical() }}
    {{ sonata_seo_lang_alternates() }}

    {{ encore_entry_link_tags(entrypoint) }}

    {% block additionalHeaderElements %}{% endblock %}

    {{ encore_entry_script_tags(entrypoint) }}

</head>

<body class="{% if app.user %}logged-in{% else %}not-logged-in{% endif %} {% block bodyClass %}{% endblock %} with-top-navbar">

{% block masterContent %}{% endblock %}

<div data-controller="upload-status-bar" class="upload-status-bar d-none">
    <div class="container-fluid">
        <div class="d-flex align-items-center justify-content-between py-2">
            <div class="d-flex align-items-center">
                <i class="fas fa-cloud-upload-alt me-2"></i>
                <span data-upload-status-bar-target="count">Uploads aktiv</span>
            </div>
            <div class="progress flex-grow-1 mx-3" style="height: 8px; max-width: 200px;" data-upload-status-bar-target="progress">
                <div class="progress-bar progress-bar-striped progress-bar-animated"
                     role="progressbar"
                     aria-valuenow="0"
                     aria-valuemin="0"
                     aria-valuemax="100"
                     data-upload-status-bar-target="progressBar"></div>
            </div>
        </div>
    </div>
</div>

<div data-controller="upload-toast" class="toast-container position-fixed bottom-0 end-0 p-3" style="z-index: 1100;">
    <div data-upload-toast-target="container"></div>
</div>

{{ encore_entry_script_tags(entrypoint) }}

{% block additionalFooterElements %}{% endblock %}
</body>
</html>
