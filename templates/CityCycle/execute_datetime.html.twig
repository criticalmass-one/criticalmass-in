{% extends 'Template/StandardTemplate.html.twig' %}

{% block title %}Turnus auslösen{% endblock %}

{% form_theme form 'Form/theme.html.twig' %}

{% import '_macros/breadcrumb.html.twig' as bc %}

{% block breadcrumbs %}
    {{ bc.render(
        [
            { label: cityCycle.city.city, url: object_path(cityCycle.city) },
            { label: 'Turnusliste', url: object_path(cityCycle.city, 'caldera_criticalmass_citycycle_list') },
            { label: 'Turnus auslösen' },
        ],
        {
            homeLabel: 'Critical Mass',
            homeUrl: path('caldera_criticalmass_frontpage')
        }
    ) }}
{% endblock %}

{% block content %}
    <div class="container">
        <div class="row">
            <div class="col-12 col-md-10 offset-md-1">
                <div class="row">
                    <div class="col-12 col-md-4">
                        <div class="card mb-3 shadow-sm">
                            <div class="card-header bg-body-tertiary py-2">
                                <h6 class="mb-0 fw-semibold">
                                    🔁 Turnus
                                </h6>
                            </div>

                            <div class="card-body">
                                <dl class="mb-0">
                                    <dt>Turnus</dt>
                                    <dd>
                                        {{ ('cycle.event_date.month_week.' ~ cityCycle.weekOfMonth)|trans }}
                                        {{ ('cycle.event_date.day.' ~ cityCycle.dayOfWeek)|trans }}
                                        im Monat
                                    </dd>

                                    <dt>Uhrzeit</dt>
                                    <dd>
                                        {{ cityCycle.time|date('H:i', 'UTC') }} Uhr
                                    </dd>

                                    <dt>Treffpunkt</dt>
                                    <dd>
                                        {% if cityCycle.location %}
                                            {{ cityCycle.location }}
                                        {% else %}
                                            <em>nicht definiert</em>
                                        {% endif %}
                                    </dd>

                                    <dt>Gültigkeitsbeginn</dt>
                                    <dd>
                                        {% if cityCycle.validFrom %}
                                            {{ cityCycle.validFrom|date('d.m.Y') }}
                                        {% else %}
                                            <em>nicht definiert</em>
                                        {% endif %}
                                    </dd>

                                    <dt>Gültigkeitsende</dt>
                                    <dd>
                                        {% if cityCycle.validUntil %}
                                            {{ cityCycle.validUntil|date('d.m.Y') }}
                                        {% else %}
                                            <em>nicht definiert</em>
                                        {% endif %}
                                    </dd>
                                </dl>
                            </div>
                        </div>
                    </div>

                    <div class="col-12 col-md-8">
                        <div class="row">
                            <div class="col-12">
                                <h2>
                                    Touren automatisch generieren
                                </h2>

                                <p class="lead">
                                    Du kannst anhand des ausgewählten Turnus’ die Touren in deiner Stadt generieren
                                    lassen.
                                </p>

                                <div class="alert alert-info">
                                    <p>
                                        Normalerweise wird diese Generierung in regelmäßigen Abständen serverseitig
                                        ausgeführt und du musst überhaupt nichts weiter dafür tun.
                                    </p>

                                    <p>
                                        Diese „manuelle Generierung“ ist für dich in der Regel nur dann interessant,
                                        wenn du
                                    <ul>
                                        <li>
                                            gerade einen neuen Turnus angelegt hast und schon einmal für die nächsten
                                            Monate Touren anlegen möchtest, bevor die automatische Generierung beginnt
                                            oder
                                        </li>
                                        <li>
                                            du oder jemand anders <strong>{{ cityCycle.city.city }}</strong> in unserer
                                            Datenbank
                                            angelegt hast und nun rückwirkend die Touren generieren lassen möchtest,
                                            weil es deine Critical Mass bereits viel länger gibt als den Eintrag in
                                            unserer Datenbank
                                        </li>
                                    </ul>
                                    </p>
                                </div>

                                <p>
                                    Mit den folgenden beiden Eingabefeldern kannst du einen Zeitraum definieren, für den
                                    die Touren anhand der Daten aus dem Turnus generiert werden sollen. Der Generator
                                    wird Monat für Monat eine einzelne Tour anlegen. Du kannst die Touren im nächsten
                                    Schritt in einer Vorschau überprüfen, bevor sie abgespeichert werden.
                                </p>
                            </div>
                        </div>

                        {{ form_start(form) }}
                        <div class="row">
                            <div class="col-12">
                                <div class="mb-3{% if form_errors(form.fromDate) %} {% endif %}">
                                    <label class="form-label" for="">Startdatum:</label>
                                    {{ form_errors(form.fromDate) }}
                                    {{ form_widget(form.fromDate) }}
                                    <p class="form-text">
                                        Bitte im Format <code>dd.mm.jjjj</code> angeben.
                                    </p>
                                </div>
                            </div>
                        </div>

                        <div class="row">
                            <div class="col-12">
                                <div class="mb-3{% if form_errors(form.untilDate) %} {% endif %}">
                                    <label class="form-label" for="">Enddatum:</label>
                                    {{ form_errors(form.untilDate) }}
                                    {{ form_widget(form.untilDate) }}
                                    <p class="form-text">
                                        Bitte im Format <code>dd.mm.jjjj</code> angeben.
                                    </p>
                                </div>
                            </div>
                        </div>

                        <div class="row">
                            <div class="col-12">
                                <div class="mb-3">
                                    {{ form_widget(form.submit, {
                                        'label': 'Weiter zur Vorschau',
                                        'attr' : { 'class': 'btn btn-primary float-end' }
                                    }) }}
                                </div>
                            </div>
                        </div>
                        {{ form_end(form) }}
                    </div>
                </div>
            </div>
        </div>
    </div>
{% endblock %}
