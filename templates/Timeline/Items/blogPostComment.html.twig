<div class="row mt-3">
    {{ include('Timeline/_includes/user_column.html.twig') }}

    <div class="col-9 col-md-10">
        <div class="row">
            <div class="col-12">
                <strong>
                    {{ item.user.username }}
                </strong>
                hat auf ein Blog-Artikel geantwortet:
            </div>
        </div>

        <div class="row">
            <div class="col-12">
                <a href="{{ objectPath(item.blogPost) }}">
                    <h4>
                        {{ item.blogPostTitle }}
                    </h4>
                </a>
            </div>
        </div>

        <div class="row">
            <div class="col-12">
                <p>
                    {{ item.text|slice(0, 140)|markdown }}…
                </p>
            </div>
        </div>
    </div>
</div>
