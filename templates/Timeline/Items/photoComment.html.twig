<div class="row mt-3">
    {{ include('Timeline/_includes/user_column.html.twig') }}

    <div class="col-9 col-md-10">
        <div class="row">
            <div class="col-12">
                <strong>
                    {{ item.user.username }}
                </strong>
                hat {{ item.postList|length }} Fotos kommentiert:
            </div>
        </div>

        <div class="row">
            <div class="col-12">
                <a href="{{ object_path(item.ride) }}">
                    <h4>
                        {{ item.ride.title }}
                    </h4>
                </a>
            </div>
        </div>

        {% for post in item.postList %}
        <div class="row">
            <div class="col-4">
                <a href="{{ object_path(post.photo) }}#post-{{ post.id }}">
                    <img src="{{ vich_uploader_asset(post.photo, 'imageFile')|imagine_filter('gallery_photo_thumb') }}" class="img-fluid img-thumbnail" />
                </a>
            </div>

            <div class="col-8">
                <p>
                    {{ post.message|markdown }}
                </p>
            </div>
        </div>
        {% endfor %}
    </div>
</div>
