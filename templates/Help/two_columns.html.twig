{% extends 'Template/StandardTemplate.html.twig' %}

{% import 'Help/_macro.html.twig' as categoryMacro %}
{% import 'Bootstrap/bootstrap_macros.html.twig' as bootstrap %}

{% block title %}{{ mainCategory.title }}{% endblock %}

{% block breadcrumb %}
    {{ bootstrap.breadcrumb_item(mainCategory.title, '', true) }}
{% endblock %}

{% block content %}
    <div class="container">
        <div class="row">
            <div class="col-12">
                <h1>
                    {{ mainCategory.title }}
                </h1>

                {% if mainCategory.intro %}
                    <p class="lead">
                        {{ mainCategory.intro }}
                    </p>
                {% endif %}
            </div>
        </div>

        <div class="row">
            <div class="col-6">
                {% for category in mainCategory.children %}
                    {% if category.side == 'left' %}
                        {{ categoryMacro.renderCategory(category) }}
                    {% endif %}
                {% endfor %}
            </div>

            <div class="col-6">
                {% for category in mainCategory.children %}
                    {% if category.side == 'right' %}
                        {{ categoryMacro.renderCategory(category) }}
                    {% endif %}
                {% endfor %}
            </div>
        </div>
    </div>
{% endblock %}
