<nav aria-label="Kalender Navigation">
    <ul class="pagination justify-content-between">
        <li class="page-item">
            <a class="page-link" href="{{ path('caldera_criticalmass_calendar', { 'year': previousMonth|date('Y'), 'month': previousMonth|date('n') }) }}">
                &larr; {{ previousMonth|format_datetime(pattern='MMMM yyyy') }}
            </a>
        </li>

        <li class="page-item">
            <a class="page-link" href="{{ path('caldera_criticalmass_calendar', { 'year': nextMonth|date('Y'), 'month': nextMonth|date('n') }) }}">
                {{ nextMonth|format_datetime(pattern='MMMM yyyy') }} &rarr;
            </a>
        </li>
    </ul>
</nav>
