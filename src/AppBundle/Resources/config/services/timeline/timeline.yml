services:
    _defaults:
        autoconfigure: true
        autowire: true
        public: true
        # this works for Symfony 3.4
        # bind:
        #    $ttl: '%timeline.ttl%'

    AppBundle\Timeline\Timeline: ~
    AppBundle\Timeline\CachedTimeline:
        arguments: ['@doctrine', '@templating', '%timeline.ttl%']