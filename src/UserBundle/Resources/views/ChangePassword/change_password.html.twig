{% extends 'AppBundle:Template:StandardTemplate.html.twig' %}

{% block title %}Kennwort ändern{% endblock %}

{% block breadcrumb %}
    <li>
        <a href="{{ path('fos_user_profile_show') }}">
            Dein Profil
        </a>
    </li>
    <li class="active">
        Kennwort ändern
    </li>
{% endblock %}

{% block content %}
    <div class="container">
        <div class="row">
            <div class="col-md-10 col-md-offset-1">
                <h2>
                    Kennwort ändern
                </h2>
            </div>
        </div>

        <div class="row">
            <div class="col-md-10 col-md-offset-1">
                <p>
                    Ändere hier dein Kennwort. Ein sicheres Kennwort besteht, wie dir sicher schon auf Dutzenden Webseiten erklärt wurde, aus Groß- und Kleinbuchstaben mit ein paar eingestreuten Ziffern und Sonderzeichen.
                </p>

                <p>
                    Falls du dein Kennwort vergessen haben solltest, kannst du dir <a href="{{ path('fos_user_resetting_request') }}">ein neues zusenden lassen</a>.
                </p>

            </div>
        </div>

        {{ form_start(form) }}

        {{ form_errors(form) }}

        <div class="row">
            <div class="col-md-10 col-md-offset-1 form-group">
                <label class="col-sm-4 control-label" for="fos_user_change_password_form[current_password]">
                    Dein aktuelles Kennwort:
                </label>
                <div class="col-sm-8">
                    {{ form_widget(form.current_password, { attr: { class: 'form-control' } }) }}
                </div>
            </div>
        </div>

        <div class="row">
            <div class="col-md-10 col-md-offset-1 form-group">
                <label class="col-sm-4 control-label" for="fos_user_change_password_form[new][first]">
                    Dein neues Kennwort:
                </label>
                <div class="col-sm-8">
                    {{ form_widget(form.plainPassword.first, { attr: { class: 'form-control' } }) }}
                </div>
            </div>
        </div>

        <div class="row">
            <div class="col-md-10 col-md-offset-1 form-group">
                <label class="col-sm-4 control-label" for="fos_user_change_password_form[new][second]">
                    Gleich noch einmal:
                </label>
                <div class="col-sm-8">
                    {{ form_widget(form.plainPassword.second, { attr: { class: 'form-control' } }) }}
                </div>
            </div>
        </div>

        <div class="row">
            <div class="col-md-10 col-md-offset-1 form-group">
                <div class="btn-group pull-right">
                    <a class="btn btn-default" href="{{ path('fos_user_profile_show') }}">
                        Abbrechen
                    </a>

                    <button type="submit" class="btn btn-success">
                        <i class="fa fa-lock"></i>
                        Kennwort ändern
                    </button>
                </div>
            </div>
        </div>

        {{ form_end(form) }}

    </div>
{% endblock %}
